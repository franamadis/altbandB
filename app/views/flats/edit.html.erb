<%= render 'shared/navbar' %>
<div>
    <div class="banner" style="background-image: url('<%= cl_image_path @flat.photo, height: 300, width: 400, crop: :fill %>');">
    <div class="banner-content">
      <h1>Edit your Flat</h1>
      <%= render 'form' %>
      <hr>

      <%= link_to "Flats", flats_path %>
    </div>
  </div>
</div>
